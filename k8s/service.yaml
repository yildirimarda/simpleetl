apiVersion: v1
kind: Service
metadata:
  name: simpleetl-service
  namespace: simpleetl
  labels:
    app: simpleetl
spec:
  selector:
    app: simpleetl
  ports:
  - name: metrics
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP